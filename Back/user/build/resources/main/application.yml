spring:
  datasource: # mysql
    url: jdbc:mysql://k7c203.p.ssafy.io:9010/user?serverTimezone=Asia/Seoul&serverTimezone=UTC&useUnicode=true&characterEncoding=utf8&allowPublicKeyRetrieval=true&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: themef1b4!
  jpa: #JPA
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      ddl-auto: update
      use-new-id-generator-mappings: false
    properties:
      hibernate:
        format_sql: true
  mvc: # emoji
    pathmatch:
      matching-strategy: ant_path_matcher
#logging:
#  level:
#    root: debug
server:
  port: 8010
